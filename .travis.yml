language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t naveen735363/docker-react -f Dockerfile.dev . 

script:
  - docker run -e CI=true  naveen735363/docker-react npm run test   